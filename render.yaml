services:
  - type: web
    name: universal-platform-backend
    runtime: node
    rootDir: .
    buildCommand: npm ci
    startCommand: npm run build && node dist/main.js
    envVars:
      - key: NODE_ENV
        value: production
